---
import MainLayout from "../layouts/MainLayout.astro"
import '../styles/main.css';
import '../styles/slideshow.css';
import data from "./db.json"



const stills = data.es.home.stills;


/*{stills.map((still) => 
  
  still.image.id = "https://ucarecdn.com/"+still.image.id+"/"
)}*/

console.log(stills)


let {title} = Astro.props; 

---

<MainLayout title="Tropical Lluvioso  ">
<div class="container-slides">
     
      {stills.map((still) =>  
      <img loading="lazy" class="slide" src={`/production/${person.gender}`} alt={still.image.id}
      >)}
      </div>
       <p class="pb-data"></p>
  
</MainLayout>


<script src="../components/auto-slideshow.js"></script>


    
