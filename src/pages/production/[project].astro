---

export async function getStaticPaths() {


const response = await fetch('https://randomuser.me/api/?results=6');
const data = await response.json();
const dataRes = data.results;


  return dataRes.map(({ gender, name, email }) => {
  console.log({gender})
    return {
      params: { gender },
      props: { name, email },
    };
  });
}

const { gender} = Astro.params;
const { name, email } = Astro.props;


---
<html>
<head>
  <title>{gender}</title>
</head>
<body>
  <h1>{gender}</h1>
  <p>{email}</p>
</body>
</html>